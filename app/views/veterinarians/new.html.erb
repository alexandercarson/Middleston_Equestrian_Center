<% if user_signed_in? && current_user.admin? %>
  <%= simple_form_for @veterinarian do |f| %>
  <% if @veterinarian.errors.any? %>
  <div id="error_explanation-veterinarian">
          <h2><%= pluralize(@veterinarian.errors.count, "error") %> prohibited this veterinarian from being saved:</h2>
          <ul>
          <% @veterinarian.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>
    <% end %>
<% end %>


<div class='container'>
  <%= bootstrap_form_for(@veterinarian) do |f| %>
    <%= f.text_area :name, required: true %>
    <%= f.telephone_field :phone_number, required: true %>
    <%= f.text_area :address, required: true %>
    <%= f.text_area :city, required: true %>
    <%= f.select :state, State.all.collect { |s| [s.state_name, s.state_code] } %>
    <%= f.number_field :zip_code, required: true %>
    <%= f.submit "Submit", class: 'btn btn-success' %> <%= link_to "Back", :back, class: 'btn btn-outline-secondary' %>
    <br />
    <br />
  <% end %>
</div>