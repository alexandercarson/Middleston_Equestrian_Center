<br>
<%= bootstrap_form_for(@medication) do |f| %>
<% if @medication.errors.any? %>
<div id="error_explanation-problem">
        <h2><%= pluralize(@medication.errors.count, "error") %> prohibited this medication from being saved:</h2>

        <ul>
        <% @medication.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  <% end %>

<div class='container'>
  <%= bootstrap_form_for(@medication) do |f| %>
  <h1></h1>
    <%= f.select :horse_id, Horse.all.collect { |h| [h.barn_name, h.id] }, required: true %>
    <%= f.text_field :medication_name %>
    <%= f.text_field :medication_for, label:'What is the medication for? (eg: Pain, Inflamation, etc..)' %>
    <%= f.time_field :medication_time, label:'What Time To Give The Medication' %>
    <%= f.text_field :medication_day, label:'What Day To Give The Medication' %>
    <%= f.text_area  :medication_frequency, label:'Medication Frequency (eg. Daily, Hourly, Weekly, etc..)' %>
    <%= f.text_field :medication_dosage %>
    <%= f.text_area  :medication_notes, label: 'Any Additional Notes?' %>
    <%= f.submit "Submit Medication To Horse", class: 'btn btn-success' %>
    <br />
    <br />
  <% end %>
</div>